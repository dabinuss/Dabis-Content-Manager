<UserControl x:Class="DCM.App.Views.TemplatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:core="clr-namespace:DCM.Core.Models;assembly=DCM.Core"
             xmlns:config="clr-namespace:DCM.Core.Configuration;assembly=DCM.Core"
             xmlns:ap="clr-namespace:DCM.App.Infrastructure.AttachedProperties">
  <Grid Margin="0,0,8,0">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="260"/>
      <ColumnDefinition Width="{StaticResource SpacingL}"/>
      <ColumnDefinition Width="*"
                        MaxWidth="480"/>
    </Grid.ColumnDefinitions>

    <GroupBox Header="{DynamicResource Templates.Header}"
              Grid.Row="0"
              Grid.Column="0">
      <DockPanel>
        <ListBox x:Name="TemplateListBox"
                 DisplayMemberPath="Name"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 SelectionChanged="TemplateListBox_SelectionChanged"/>
      </DockPanel>
    </GroupBox>

    <GroupBox Header="{DynamicResource Templates.Edit}"
              Grid.Row="0"
              Grid.Column="2">
      <Grid>
        <Grid x:Name="TemplateEditorGrid">
          <Grid.Style>
            <Style TargetType="Grid">
              <Setter Property="IsEnabled"
                      Value="True"/>
              <Setter Property="Opacity"
                      Value="1"/>
              <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=TemplateListBox, Path=SelectedItem}"
                             Value="{x:Null}">
                  <Setter Property="IsEnabled"
                          Value="False"/>
                  <Setter Property="Opacity"
                          Value="0.6"/>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Grid.Style>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="90"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="{StaticResource SpacingM}"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="{StaticResource SpacingM}"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="{StaticResource SpacingM}"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="{StaticResource SpacingL}"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="{StaticResource SpacingS}"/>
          <RowDefinition Height="*"
                         MinHeight="120"/>
          <RowDefinition Height="{StaticResource SpacingL}"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="{StaticResource SpacingS}"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="{DynamicResource Templates.Fields.Name}"
               Grid.Row="0"
               Grid.Column="0"/>
        <TextBox x:Name="TemplateNameTextBox"
                 Grid.Row="0"
                 Grid.Column="1"/>

        <Label Content="{DynamicResource Templates.Fields.Platform}"
               Grid.Row="2"
               Grid.Column="0"/>
        <ComboBox x:Name="TemplatePlatformComboBox"
                  Grid.Row="2"
                  Grid.Column="1"
                  HorizontalAlignment="Left"
                  MinWidth="150"/>

        <CheckBox x:Name="TemplateIsDefaultCheckBox"
                  Grid.Row="4"
                  Grid.Column="0"
                  Grid.ColumnSpan="2"
                  Content="{DynamicResource Templates.Fields.UseAsStandard}"/>

        <Label Content="{DynamicResource Templates.Fields.Notes}"
               Grid.Row="6"
               Grid.Column="0"/>
        <TextBox x:Name="TemplateDescriptionTextBox"
                 Grid.Row="6"
                 Grid.Column="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Height="40"
                 VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="8"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   Text="{DynamicResource Templates.Fields.TemplateBody}"
                   Foreground="{StaticResource TextMutedBrush}"
                   FontSize="{StaticResource FontSizeM}"/>
        <TextBox x:Name="TemplateBodyEditorTextBox"
                 Grid.Row="10"
                 Grid.Column="0"
                 Grid.ColumnSpan="2"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>

        <Grid Grid.Row="12"
              Grid.Column="0"
              Grid.ColumnSpan="2">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="{StaticResource SpacingS}"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>
          <TextBlock Grid.Row="0"
                     Text="{DynamicResource Templates.Placeholders.Insert}"
                     FontSize="{StaticResource FontSizeS}"
                     Foreground="{StaticResource TextMutedBrush}"/>
          <ItemsControl x:Name="PlaceholderItemsControl"
                        Grid.Row="2">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Content="{Binding}"
                        Margin="0,0,8,8"
                        Style="{StaticResource SecondaryButton}"
                        ap:ButtonIcon.Glyph="&#xe892;"
                        Padding="8,8"
                        Click="PlaceholderButton_Click"/>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Grid>
        </Grid>

        <Border x:Name="TemplateEmptyState"
                Background="Transparent">
          <Border.Style>
            <Style TargetType="Border">
              <Setter Property="Visibility"
                      Value="Collapsed"/>
              <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=TemplateListBox, Path=SelectedItem}"
                             Value="{x:Null}">
                  <Setter Property="Visibility"
                          Value="Visible"/>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Border.Style>
          <StackPanel HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Margin="16">
            <TextBlock Text="Kein Template ausgewaehlt."
                       FontSize="{StaticResource FontSizeM}"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Waehle links ein Template oder erstelle ein neues."
                       FontSize="{StaticResource FontSizeS}"
                       Foreground="{StaticResource TextMutedBrush}"
                       Margin="0,8,0,0"
                       TextAlignment="Center"/>
            <Button Content="Neues Template"
                    Style="{StaticResource PrimaryButton}"
                    ap:ButtonIcon.Glyph="&#xe145;"
                    Click="TemplateNewButton_Click"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Center"/>
          </StackPanel>
        </Border>
      </Grid>
    </GroupBox>
  </Grid>
</UserControl>

