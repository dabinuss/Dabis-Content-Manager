<UserControl x:Class="DCM.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:core="clr-namespace:DCM.Core.Models;assembly=DCM.Core"
             xmlns:config="clr-namespace:DCM.Core.Configuration;assembly=DCM.Core">
          <Grid Margin="0,0,16,0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="{StaticResource SpacingXL}"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header mit Speichern-Button -->
            <!-- HEADER wie Accounts, mit Save-Button rechts -->
            <Grid Grid.Row="0"
                  Margin="0,0,0,16">
              <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>

              <!-- Titel + Beschreibung (wie Accounts) -->
              <StackPanel Grid.Column="0">
                <TextBlock Text="{DynamicResource Settings}"
                           FontSize="{StaticResource FontSizeXL}"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
              </StackPanel>

              <!-- Speichern-Button rechts, auf HÃƒÂ¶he des Headers -->
              <Button x:Name="SettingsSaveButton"
                      Grid.Column="1"
                      Style="{StaticResource PrimaryButton}"
                      Click="SettingsSaveButton_Click"
                      VerticalAlignment="Center"
                      Margin="4">
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{DynamicResource Settings.SaveAll}"/>
                </StackPanel>
              </Button>
            </Grid>


            <!-- ZWEI-SPALTEN-LAYOUT -->
            <Grid Grid.Row="2">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"
                                  MinWidth="350"/>
                <ColumnDefinition Width="{StaticResource SpacingXL}"/>
                <ColumnDefinition Width="*"
                                  MinWidth="350"/>
              </Grid.ColumnDefinitions>

              <!-- LINKE SPALTE -->
              <StackPanel Grid.Column="0">

                <!-- Sprache -->
                <GroupBox>
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Settings.Language}"
                               Style="{StaticResource TextLabel}"
                               Margin="0,-6,0,8"/>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="{StaticResource SpacingM}"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                      </Grid.RowDefinitions>

                      <ComboBox x:Name="LanguageComboBox"
                                Grid.Row="0"
                                Grid.Column="0"
                                Grid.ColumnSpan="3"
                                HorizontalAlignment="Left"
                                MinWidth="200"
                                SelectionChanged="LanguageComboBox_SelectionChanged"/>

                      <TextBlock Grid.Row="2"
                                 Grid.Column="0"
                                 Grid.ColumnSpan="3"
                                 Text="{DynamicResource Settings.Language.Restart.Hint}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                    </Grid>
                  </StackPanel>
                </GroupBox>

                <!-- Ordner -->
                <GroupBox>
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Settings.StandardFolders}"
                               Style="{StaticResource TextLabel}"
                               Margin="0,-6,0,8"/>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                      </Grid.ColumnDefinitions>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                      </Grid.RowDefinitions>

                      <TextBlock Grid.Row="0"
                                 Grid.Column="0"
                                 Grid.ColumnSpan="2"
                                 Text="{DynamicResource Settings.VideoFolder}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                      <TextBox x:Name="DefaultVideoFolderTextBox"
                               Grid.Row="2"
                               Grid.Column="0"
                               Margin="0,0,8,0"/>
                      <Button x:Name="DefaultVideoFolderBrowseButton"
                              Grid.Row="2"
                              Grid.Column="1"
                              Content="{DynamicResource Settings.ChoosePath}"
                              Style="{StaticResource SecondaryButton}"
                              Click="DefaultVideoFolderBrowseButton_Click"/>

                      <TextBlock Grid.Row="4"
                                 Grid.Column="0"
                                 Grid.ColumnSpan="2"
                                 Text="{DynamicResource Settings.ThumbnailFolder}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                      <TextBox x:Name="DefaultThumbnailFolderTextBox"
                               Grid.Row="6"
                               Grid.Column="0"
                               Margin="0,0,8,0"/>
                      <Button x:Name="DefaultThumbnailFolderBrowseButton"
                              Grid.Row="6"
                              Grid.Column="1"
                              Content="{DynamicResource Settings.ChoosePath}"
                              Style="{StaticResource SecondaryButton}"
                              Click="DefaultThumbnailFolderBrowseButton_Click"/>
                    </Grid>
                  </StackPanel>
                </GroupBox>

                <!-- Upload-Standardwerte -->
                <GroupBox>
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Settings.UploadSettings}"
                               Style="{StaticResource TextLabel}"
                               Margin="0,-6,0,8"/>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="{StaticResource SpacingM}"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingL}"/>
                        <RowDefinition Height="Auto"/>
                      </Grid.RowDefinitions>

                      <TextBlock Grid.Row="0"
                                 Grid.Column="0"
                                 Text="{DynamicResource Settings.StandardReleaseTime}"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <TextBox x:Name="DefaultSchedulingTimeTextBox"
                               Grid.Row="0"
                               Grid.Column="2"
                               HorizontalAlignment="Left"
                               Width="90"
                               Text="18:00"/>

                      <TextBlock Grid.Row="2"
                                 Grid.Column="0"
                                 Text="{DynamicResource Upload.Fields.Visibility}"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <ComboBox x:Name="DefaultVisibilityComboBox"
                                Grid.Row="2"
                                Grid.Column="2"
                                HorizontalAlignment="Left"
                                MinWidth="160">
                        <ComboBoxItem Content="{DynamicResource Upload.Fields.Visibility.Public}"
                                      Tag="{x:Static core:VideoVisibility.Public}"/>
                        <ComboBoxItem Content="{DynamicResource Upload.Fields.Visibility.Unlisted}"
                                      Tag="{x:Static core:VideoVisibility.Unlisted}"/>
                        <ComboBoxItem Content="{DynamicResource Upload.Fields.Visibility.Private}"
                                      Tag="{x:Static core:VideoVisibility.Private}"/>
                      </ComboBox>

                      <StackPanel Grid.Row="4"
                                  Grid.Column="0"
                                  Grid.ColumnSpan="3">
                        <CheckBox x:Name="ConfirmBeforeUploadCheckBox"
                                  Content="{DynamicResource Settings.StandardReleaseTime}"/>
                        <CheckBox x:Name="AutoApplyDefaultTemplateCheckBox"
                                  Content="{DynamicResource Settings.ConfirmUpload}"/>
                        <CheckBox x:Name="OpenBrowserAfterUploadCheckBox"
                                  Content="{DynamicResource Settings.OpenBrowserAfterUpload}"/>
                        <CheckBox x:Name="AutoConnectYouTubeCheckBox"
                                  Content="{DynamicResource Settings.AutomaticConnect}"/>
                      </StackPanel>
                    </Grid>
                  </StackPanel>
                </GroupBox>

                <!-- Transkription -->
                <GroupBox>
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Settings.Transcription}"
                               Style="{StaticResource TextLabel}"
                               Margin="0,-6,0,8"/>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="{StaticResource SpacingM}"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingL}"/>
                        <RowDefinition Height="Auto"/>
                      </Grid.RowDefinitions>

                      <CheckBox x:Name="TranscriptionAutoCheckBox"
                                Grid.Row="0"
                                Grid.Column="0"
                                Grid.ColumnSpan="3"
                                Content="{DynamicResource Settings.AutomaticTranscription}"/>

                      <TextBlock Grid.Row="2"
                                 Grid.Column="0"
                                 Text="{DynamicResource Settings.WhisperModelSize}"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <ComboBox x:Name="TranscriptionModelSizeComboBox"
                                Grid.Row="2"
                                Grid.Column="2"
                                HorizontalAlignment="Left"
                                MinWidth="200"
                                SelectionChanged="TranscriptionModelSizeComboBox_SelectionChanged">
                        <ComboBoxItem Content="{DynamicResource Settings.WhisperModelSize.Tiny}"
                                      Tag="{x:Static config:WhisperModelSize.Tiny}"/>
                        <ComboBoxItem Content="{DynamicResource Settings.WhisperModelSize.Base}"
                                      Tag="{x:Static config:WhisperModelSize.Base}"/>
                        <ComboBoxItem Content="{DynamicResource Settings.WhisperModelSize.Small}"
                                      Tag="{x:Static config:WhisperModelSize.Small}"
                                      IsSelected="True"/>
                        <ComboBoxItem Content="{DynamicResource Settings.WhisperModelSize.Medium}"
                                      Tag="{x:Static config:WhisperModelSize.Medium}"/>
                        <ComboBoxItem Content="{DynamicResource Settings.WhisperModelSize.Large}"
                                      Tag="{x:Static config:WhisperModelSize.Large}"/>
                      </ComboBox>

                      <TextBlock Grid.Row="4"
                                 Grid.Column="0"
                                 Text="{DynamicResource Settings.WhisperLanguage}"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <ComboBox x:Name="TranscriptionLanguageComboBox"
                                Grid.Row="4"
                                Grid.Column="2"
                                HorizontalAlignment="Left"
                                MinWidth="200">
                        <ComboBoxItem Content="{DynamicResource Settings.WhisperLanguage.De}"
                                      Tag="de"
                                      IsSelected="True"/>
                        <ComboBoxItem Content="{DynamicResource Settings.WhisperLanguage.En}"
                                      Tag="en"/>
                        <ComboBoxItem Content="{DynamicResource Settings.WhisperLanguage.Detect}"
                                      Tag="auto"/>
                      </ComboBox>

                      <TextBlock Grid.Row="6"
                                 Grid.Column="0"
                                 Text="{DynamicResource Common.Status}"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <TextBlock x:Name="TranscriptionStatusTextBlock"
                                 Grid.Row="6"
                                 Grid.Column="2"
                                 VerticalAlignment="Center"
                                 Text="{DynamicResource Settings.Msg.NotChecked}"
                                 Foreground="{StaticResource TextMutedBrush}"/>

                      <StackPanel Grid.Row="8"
                                  Grid.Column="0"
                                  Grid.ColumnSpan="3"
                                  Orientation="Horizontal">
                        <Button x:Name="TranscriptionDownloadButton"
                                Content="{DynamicResource Settings.Whisper.Download}"
                                Style="{StaticResource SecondaryButton}"
                                Click="TranscriptionDownloadButton_Click"/>
                        <ProgressBar x:Name="TranscriptionDownloadProgressBar"
                                     Width="150"
                                     Height="20"
                                     Minimum="0"
                                     Maximum="100"
                                     Visibility="Collapsed"
                                     Margin="8,0,0,0"/>
                      </StackPanel>
                    </Grid>
                  </StackPanel>
                </GroupBox>

              </StackPanel>

              <!-- RECHTE SPALTE -->
              <StackPanel Grid.Column="2">

                <!-- KI / LLM -->
                <GroupBox>
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Settings.LLM}"
                               Style="{StaticResource TextLabel}"
                               Margin="0,-6,0,8"/>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                      </Grid.ColumnDefinitions>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                      </Grid.RowDefinitions>

                      <TextBlock Grid.Row="0"
                                 Grid.Column="0"
                                 Grid.ColumnSpan="2"
                                 Text="{DynamicResource Settings.LLM.Mode}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                      <ComboBox x:Name="LlmModeComboBox"
                                Grid.Row="2"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                HorizontalAlignment="Left"
                                MinWidth="180"
                                SelectionChanged="LlmModeComboBox_SelectionChanged">
                        <ComboBoxItem Content="{DynamicResource Settings.LLM.Off}"
                                      Tag="Off"/>
                        <ComboBoxItem Content="{DynamicResource Settings.LLM.Local}"
                                      Tag="Local"/>
                      </ComboBox>

                      <TextBlock Grid.Row="4"
                                 Grid.Column="0"
                                 Grid.ColumnSpan="2"
                                 Text="{DynamicResource Settings.LLM.PathToModel}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                      <TextBox x:Name="LlmModelPathTextBox"
                               Grid.Row="6"
                               Grid.Column="0"
                               Margin="0,0,8,0"
                               ToolTip="Pfad zur GGUF-Modelldatei"/>
                      <Button x:Name="LlmModelPathBrowseButton"
                              Grid.Row="6"
                              Grid.Column="1"
                              Content="{DynamicResource Settings.ChoosePath}"
                              Style="{StaticResource SecondaryButton}"
                              Click="LlmModelPathBrowseButton_Click"/>

                      <TextBlock Grid.Row="8"
                                 Grid.Column="0"
                                 Grid.ColumnSpan="2"
                                 Text="{DynamicResource Settings.LLM.ModelType}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                      <ComboBox x:Name="LlmModelTypeComboBox"
                                Grid.Row="10"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                HorizontalAlignment="Left"
                                MinWidth="180"
                                ToolTip="{DynamicResource Settings.LLM.ModelType.Tooltip}">
                        <ComboBoxItem Content="{DynamicResource Settings.LLM.ModelType.Detect}"
                                      Tag="Auto"
                                      IsSelected="True"/>
                        <ComboBoxItem Content="{DynamicResource Settings.LLM.ModelType.Phi3}"
                                      Tag="Phi3"/>
                        <ComboBoxItem Content="{DynamicResource Settings.LLM.ModelType.Mistral3}"
                                      Tag="Mistral3"/>
                      </ComboBox>

                      <TextBlock Grid.Row="12"
                                 Grid.Column="0"
                                 Grid.ColumnSpan="2"
                                 Text="{DynamicResource Settings.LLM.ModelType.SystemPrompt}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                      <TextBox x:Name="LlmSystemPromptTextBox"
                               Grid.Row="14"
                               Grid.Column="0"
                               Grid.ColumnSpan="2"
                               AcceptsReturn="True"
                               TextWrapping="Wrap"
                               Height="80"
                               VerticalScrollBarVisibility="Auto"
                               ToolTip="Globaler System-Prompt, der dem Modell als Kontext ÃƒÂ¼bergeben wird"/>

                      <StackPanel Grid.Row="16"
                                  Grid.Column="0"
                                  Grid.ColumnSpan="2"
                                  Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Common.Status}"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   Margin="0,0,8,0"/>
                        <TextBlock x:Name="LlmStatusTextBlock"
                                   Text="{DynamicResource Settings.LLM.Status}"
                                   Foreground="{StaticResource TextMutedBrush}"/>
                      </StackPanel>
                    </Grid>
                  </StackPanel>
                </GroupBox>

                <!-- Generierungs-Parameter -->
                <GroupBox>
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Settings.LLM.Parameters}"
                               Style="{StaticResource TextLabel}"
                               Margin="0,-6,0,8"/>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="{StaticResource SpacingM}"/>
                        <ColumnDefinition Width="*"/>
                      </Grid.ColumnDefinitions>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                      </Grid.RowDefinitions>

                      <TextBlock Grid.Row="0"
                                 Grid.Column="0"
                                 Text="{DynamicResource Settings.LLM.Parameters.MaxToken}"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <StackPanel Grid.Row="0"
                                  Grid.Column="2"
                                  Orientation="Horizontal">
                        <TextBox x:Name="LlmMaxTokensTextBox"
                                 Width="70"
                                 Text="256"/>
                        <TextBlock VerticalAlignment="Center"
                                   Margin="8,0,0,0"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   FontSize="{StaticResource FontSizeS}"
                                   Text="(64-1024)"/>
                      </StackPanel>

                      <TextBlock Grid.Row="2"
                                 Grid.Column="0"
                                 Text="{DynamicResource Settings.LLM.Parameters.Temperature}"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <StackPanel Grid.Row="2"
                                  Grid.Column="2"
                                  Orientation="Horizontal">
                        <Slider x:Name="LlmTemperatureSlider"
                                Width="140"
                                Minimum="0"
                                Maximum="1"
                                Value="0.3"
                                TickFrequency="0.1"
                                IsSnapToTickEnabled="True"
                                ValueChanged="LlmTemperatureSlider_ValueChanged"/>
                        <TextBlock x:Name="LlmTemperatureValueText"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0"
                                   Width="30"
                                   Text="0.3"/>
                      </StackPanel>
                    </Grid>
                  </StackPanel>
                </GroupBox>

                <!-- Prompt-Anweisungen -->
                <GroupBox>
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Settings.LLM.Prompts}"
                               Style="{StaticResource TextLabel}"
                               Margin="0,-6,0,8"/>
                    <Grid>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingM}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                      </Grid.RowDefinitions>

                      <TextBlock Grid.Row="0"
                                 Text="{DynamicResource Settings.LLM.Prompts.Title}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                      <TextBox x:Name="LlmTitleCustomPromptTextBox"
                               Grid.Row="2"
                               AcceptsReturn="True"
                               TextWrapping="Wrap"
                               Height="50"
                               VerticalScrollBarVisibility="Auto"/>

                      <TextBlock Grid.Row="4"
                                 Text="{DynamicResource Settings.LLM.Prompts.Description}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                      <TextBox x:Name="LlmDescriptionCustomPromptTextBox"
                               Grid.Row="6"
                               AcceptsReturn="True"
                               TextWrapping="Wrap"
                               Height="50"
                               VerticalScrollBarVisibility="Auto"/>

                      <TextBlock Grid.Row="8"
                                 Text="{DynamicResource Settings.LLM.Prompts.Tags}"
                                 FontSize="{StaticResource FontSizeS}"
                                 Foreground="{StaticResource TextMutedBrush}"/>
                      <TextBox x:Name="LlmTagsCustomPromptTextBox"
                               Grid.Row="10"
                               AcceptsReturn="True"
                               TextWrapping="Wrap"
                               Height="50"
                               VerticalScrollBarVisibility="Auto"/>
                    </Grid>
                  </StackPanel>
                </GroupBox>

                <!-- Vorschlagsanzahl -->
                <GroupBox>
                  <StackPanel>
                    <TextBlock Text="Vorschlagsanzahl"
                               Style="{StaticResource TextLabel}"
                               Margin="0,-6,0,8"/>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                      </Grid.ColumnDefinitions>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="{StaticResource SpacingS}"/>
                        <RowDefinition Height="Auto"/>
                      </Grid.RowDefinitions>

                      <TextBlock Grid.Row="0"
                                 Grid.Column="0"
                                 Text="Titel"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <ComboBox x:Name="TitleSuggestionCountComboBox"
                                Grid.Row="0"
                                Grid.Column="1"
                                Width="80"
                                HorizontalAlignment="Left">
                        <ComboBoxItem Content="1"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                      </ComboBox>

                      <TextBlock Grid.Row="2"
                                 Grid.Column="0"
                                 Text="Beschreibung"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <ComboBox x:Name="DescriptionSuggestionCountComboBox"
                                Grid.Row="2"
                                Grid.Column="1"
                                Width="80"
                                HorizontalAlignment="Left">
                        <ComboBoxItem Content="1"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                      </ComboBox>

                      <TextBlock Grid.Row="4"
                                 Grid.Column="0"
                                 Text="Tags"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                      <ComboBox x:Name="TagsSuggestionCountComboBox"
                                Grid.Row="4"
                                Grid.Column="1"
                                Width="80"
                                HorizontalAlignment="Left">
                        <ComboBoxItem Content="1"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                      </ComboBox>
                    </Grid>
                  </StackPanel>
                </GroupBox>

              </StackPanel>
            </Grid>
          </Grid>
</UserControl>
