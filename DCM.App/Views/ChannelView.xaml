<UserControl x:Class="DCM.App.Views.ChannelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:core="clr-namespace:DCM.Core.Models;assembly=DCM.Core"
             xmlns:config="clr-namespace:DCM.Core.Configuration;assembly=DCM.Core">
<!-- wie PageUpload: Grid füllt die Breite, nur ein rechter Margin -->
<Grid Margin="{StaticResource ViewRootMargin}">

  <Grid.RowDefinitions>
    <RowDefinition Height="Auto"/>
    <!-- Inhalt -->
  </Grid.RowDefinitions>

  <!-- ZWEI-SPALTEN-LAYOUT wie bei Upload -->
  <Grid Grid.Row="0"
        VerticalAlignment="Top">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"
                        MinWidth="350"/>
      <ColumnDefinition Width="{StaticResource SpacingXXL}"/>
      <ColumnDefinition Width="*"
                        MinWidth="300"
                        MaxWidth="400"/>
    </Grid.ColumnDefinitions>

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <!-- obere Boxen -->
      <RowDefinition Height="{StaticResource SpacingXL}"/>
      <!-- Abstand -->
      <RowDefinition Height="Auto"/>
      <!-- untere Box -->
    </Grid.RowDefinitions>

    <!-- BOX 1: Kanalprofil (links) -->
    <GroupBox Grid.Row="0"
              Grid.Column="0">
      <GroupBox.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Persona.Profile}" VerticalAlignment="Center"/>
          <Border Grid.Column="1" Style="{StaticResource HelpIconButton}"
                  ToolTip="{DynamicResource Channel.Tooltip.Profile.Example}">
            <TextBlock Style="{StaticResource HelpIconGlyph}"/>
          </Border>
        </Grid>
      </GroupBox.Header>
      <Grid Margin="8">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name mit Help-Icon -->
        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Fields.Name}"
                     Style="{StaticResource TextLabel}"/>
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Border x:Name="NameValidationIcon" Visibility="Collapsed" VerticalAlignment="Center" Margin="0,0,4,0">
              <TextBlock x:Name="NameValidationIconGlyph" Style="{StaticResource ValidationIcon}"/>
            </Border>
            <Border Style="{StaticResource HelpIconButton}"
                    ToolTip="{DynamicResource Channel.Example.Name}">
              <TextBlock Style="{StaticResource HelpIconGlyph}"/>
            </Border>
          </StackPanel>
        </Grid>
        <TextBox Grid.Row="1"
                 x:Name="ChannelPersonaNameTextBox"
                 Margin="{StaticResource LabelMarginBottomL}"
                 TextChanged="OnSettingChanged"/>

        <!-- Kanalname mit Help-Icon -->
        <Grid Grid.Row="2">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Fields.ChannelName}"
                     Style="{StaticResource TextLabel}"/>
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Border x:Name="ChannelNameValidationIcon" Visibility="Collapsed" VerticalAlignment="Center" Margin="0,0,4,0">
              <TextBlock x:Name="ChannelNameValidationIconGlyph" Style="{StaticResource ValidationIcon}"/>
            </Border>
            <Border Style="{StaticResource HelpIconButton}"
                    ToolTip="{DynamicResource Channel.Example.ChannelName}">
              <TextBlock Style="{StaticResource HelpIconGlyph}"/>
            </Border>
          </StackPanel>
        </Grid>
        <TextBox Grid.Row="3"
                 x:Name="ChannelPersonaChannelNameTextBox"
                 TextChanged="OnSettingChanged"/>
      </Grid>
    </GroupBox>

    <!-- BOX 2: Sprache & Stil (rechts) -->
    <GroupBox Grid.Row="0"
              Grid.Column="2">
      <GroupBox.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Persona.Style}" VerticalAlignment="Center"/>
          <Border Grid.Column="1" Style="{StaticResource HelpIconButton}"
                  ToolTip="{DynamicResource Channel.Tooltip.Style.Example}">
            <TextBlock Style="{StaticResource HelpIconGlyph}"/>
          </Border>
        </Grid>
      </GroupBox.Header>
      <Grid Margin="8">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Sprache mit Help-Icon -->
        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Fields.Language}"
                     Style="{StaticResource TextLabel}"/>
          <Border Grid.Column="1" Style="{StaticResource HelpIconButton}"
                  ToolTip="{DynamicResource Channel.Example.Language}">
            <TextBlock Style="{StaticResource HelpIconGlyph}"/>
          </Border>
        </Grid>
        <ComboBox Grid.Row="1"
                  x:Name="ChannelPersonaLanguageTextBox"
                  IsEditable="True"
                  IsTextSearchEnabled="True"
                  StaysOpenOnEdit="True"
                  MinWidth="{StaticResource InputMinWidthM}"
                  Margin="{StaticResource LabelMarginBottomL}"
                  SelectionChanged="OnComboBoxSettingChanged"/>

        <!-- Tonalität mit Help-Icon -->
        <Grid Grid.Row="2">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Fields.ToneOfWords}"
                     Style="{StaticResource TextLabel}"/>
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Border x:Name="ToneValidationIcon" Visibility="Collapsed" VerticalAlignment="Center" Margin="0,0,4,0">
              <TextBlock x:Name="ToneValidationIconGlyph" Style="{StaticResource ValidationIcon}"/>
            </Border>
            <Border Style="{StaticResource HelpIconButton}"
                    ToolTip="{DynamicResource Channel.Example.Tone}">
              <TextBlock Style="{StaticResource HelpIconGlyph}"/>
            </Border>
          </StackPanel>
        </Grid>
        <TextBox Grid.Row="3"
                 x:Name="ChannelPersonaToneOfVoiceTextBox"
                 Margin="{StaticResource LabelMarginBottomL}"
                 TextChanged="OnSettingChanged"/>

        <!-- Content-Typ mit Help-Icon -->
        <Grid Grid.Row="4">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Fields.ContentType}"
                     Style="{StaticResource TextLabel}"/>
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Border x:Name="ContentTypeValidationIcon" Visibility="Collapsed" VerticalAlignment="Center" Margin="0,0,4,0">
              <TextBlock x:Name="ContentTypeValidationIconGlyph" Style="{StaticResource ValidationIcon}"/>
            </Border>
            <Border Style="{StaticResource HelpIconButton}"
                    ToolTip="{DynamicResource Channel.Example.ContentType}">
              <TextBlock Style="{StaticResource HelpIconGlyph}"/>
            </Border>
          </StackPanel>
        </Grid>
        <TextBox Grid.Row="5"
                 x:Name="ChannelPersonaContentTypeTextBox"
                 Margin="{StaticResource LabelMarginBottomL}"
                 TextChanged="OnSettingChanged"/>

        <!-- Zielgruppe mit Help-Icon -->
        <Grid Grid.Row="6">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Fields.TargetAudience}"
                     Style="{StaticResource TextLabel}"/>
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Border x:Name="TargetAudienceValidationIcon" Visibility="Collapsed" VerticalAlignment="Center" Margin="0,0,4,0">
              <TextBlock x:Name="TargetAudienceValidationIconGlyph" Style="{StaticResource ValidationIcon}"/>
            </Border>
            <Border Style="{StaticResource HelpIconButton}"
                    ToolTip="{DynamicResource Channel.Example.TargetAudience}">
              <TextBlock Style="{StaticResource HelpIconGlyph}"/>
            </Border>
          </StackPanel>
        </Grid>
        <TextBox Grid.Row="7"
                 x:Name="ChannelPersonaTargetAudienceTextBox"
                 TextChanged="OnSettingChanged"/>
      </Grid>
    </GroupBox>

    <!-- BOX 3: Zusätzliche Hinweise (unten, volle Breite) -->
    <GroupBox Grid.Row="2"
              Grid.Column="0"
              Grid.ColumnSpan="3">
      <GroupBox.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Persona.Additional}" VerticalAlignment="Center"/>
          <Border Grid.Column="1" Style="{StaticResource HelpIconButton}"
                  ToolTip="{DynamicResource Channel.Tooltip.Additional.Example}">
            <TextBlock Style="{StaticResource HelpIconGlyph}"/>
          </Border>
        </Grid>
      </GroupBox.Header>
      <Grid Margin="8">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Zusätzliche Anweisungen mit Help-Icon -->
        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{DynamicResource Channel.Fields.AdditionalInstructions}"
                     Foreground="{StaticResource TextMutedBrush}"
                     FontSize="{StaticResource FontSizeM}"/>
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Border x:Name="AdditionalValidationIcon" Visibility="Collapsed" VerticalAlignment="Center" Margin="0,0,4,0">
              <TextBlock x:Name="AdditionalValidationIconGlyph" Style="{StaticResource ValidationIcon}"/>
            </Border>
            <Border Style="{StaticResource HelpIconButton}"
                    ToolTip="{DynamicResource Channel.Tooltip.Additional.Example}">
              <TextBlock Style="{StaticResource HelpIconGlyph}"/>
            </Border>
          </StackPanel>
        </Grid>
        <TextBox Grid.Row="1"
                 x:Name="ChannelPersonaAdditionalInstructionsTextBox"
                 Margin="{StaticResource LabelMarginTop}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Height="80"
                 TextChanged="OnSettingChanged"/>
      </Grid>
    </GroupBox>

  </Grid>
</Grid>
</UserControl>
